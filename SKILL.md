---
name: gift-picker
description: 礼物挑选助手。为女朋友/老婆/心仪对象挑选礼物。收到请求后第一个动作必须是：运行 profile_manager.py list 检查已有画像。有画像时展示名称列表并让用户选择使用/更新/新建；无画像时直接问"你想怎么提供她的信息"。工作流 7 步：Step1画像 → Step2关系阶段 → Step3预算 → Step4场景 → Step5登录 → Step6采集 → Step7报告。
agent_created: true
---

# 礼物挑选助手

## 关键路径

- 画像：`~/.workbuddy/gift-picker/profiles/<昵称>.json`（环境变量 `GIFT_PICKER_HOME` 可覆盖）
- 登录态：`~/.workbuddy/gift-picker/sessions/<channel>.json`（Playwright 持久化浏览器）
- 中间数据：本 skill 目录下 `memory.json`（用后即删，支持断点恢复）
- 报告输出：当前工作区 `gift-report-<日期>.html`

## 核心工作流（7 步，严格按顺序执行，每步必须完成后才能进入下一步）

> 🔴 **收到任何礼物挑选请求后，第一个动作必须是：运行 `python scripts/profile_manager.py list` 检查画像。有画像时按"情况 2"流程展示名称列表让用户选择；无画像时按"情况 1"流程直接问"你想怎么提供她的信息"。禁止跳过此检查。**

> 🔴 **格式要求：仅当 Step 缺少所需信息时，才用一次 AskUserQuestion 工具单独完成；首条描述已明确提供的字段无需重复确认。禁止把多个仍需提问的 Step 合并在一条消息中。**

> ⚡ **首条信息直通规则**：先解析用户首条描述。若其中已明确给出关系阶段（如“热恋期”）和/或预算（如“预算 800 元”），必须直接采用该值，分别跳过 Step 2 和/或 Step 3 的对应提问；不得为了确认再次提问。阶段直接写入 `relationship_stage`，预算直接写入 `memory.json`。仅对未提供、无法识别或存在歧义的字段提问。场合已可明确识别时同样直接写入；画像仍按 Step 1 的既有流程处理。

### Step 1：建立/读取画像

运行 `python scripts/profile_manager.py list` 查看已有画像。

#### 情况 1：无画像

> ⚠️ **硬性规则**：无画像时，第一个问题**必须**是下面这个 AskUserQuestion，**禁止**直接问风格/兴趣/颜色等画像细节。

> 🔴 **必须使用 AskUserQuestion 工具发起此提问。此为独立的一轮对话，收到用户回答前禁止进入 Step 2。**

用 AskUserQuestion 问："还没她的画像，你想怎么提供信息？"
选项：
- 🔗 社媒 ID 一键画像（推荐）— 给我她的小红书/微博/抖音/B站号（仅适合已征得她同意的场景）
- ✏️ 我来描述 — 一段话告诉我她的喜好风格
- 📸 发截图给我 — 小红书收藏/穿搭照等
- ❓ 逐题问答 — 我问你答

**用户选择后按对应方式执行**（不要跳过这一步直接问画像细节）：

> 🔴 **硬性禁止规则**：选择"我来描述"/"社媒 ID"/"发截图"后，**绝对禁止**再问任何补充问题（包括但不限于礼物偏好、品类偏好、风格偏好、品牌偏好等）。所有信息必须从用户提供的材料中解析。即使信息不完整也不追问。

> ⚠️ **唯一安全例外**：过敏/忌口是安全必答项。如果用户提供的材料中未包含过敏信息，**必须**用一次 AskUserQuestion 确认："为了安全，只问这一条——她有过敏或忌口吗？"选项：没有 / 皮肤敏感 / 有食物忌口（自由输入）。此为所有路径的强制安全检查，不可省略。

> 🔒 **隐私说明**：社媒 ID 方式仅读取公开内容，不保存原文，任务结束即清理。不会分析定位信息，不会代她操作任何账号。

| 方式 | 操作 |
|------|------|
| 社媒 ID | 用 browser-use 读取公开主页 → 喜好分析 → 对话内输出不落盘报告 → 用户确认后 `set` 保存。**禁止额外提问** |
| 我来描述 | AI 解析用户描述为结构化字段 → 复述摘要 → 用户确认后 `set` 保存。**零提问，直接解析** |
| 发截图 | 解析图片提取色系/品牌/风格 → 复述摘要 → 用户确认后 `set` 保存。**禁止额外提问** |
| 逐题问答 | 按 `references/profile-schema.md` 全量问答分批提问。**仅此方式才允许提问** |

收集完成后必须向用户复述画像摘要，用户确认后保存画像。

**保存画像流程**（先确认昵称再保存）：
1. 将画像数据写入临时 JSON 文件（如 `_tmp_profile.json`）
2. **基于画像关键词自动生成建议昵称**（格式：[关系阶段] + [风格/爱好关键词]，如"热恋清新可爱""稳定摄影控"）
3. 用 AskUserQuestion 向用户展示建议昵称并确认：
   - 问题："画像昵称'XXX'，可以直接用吗？"
   - 选项："就用这个昵称" / "我想改个昵称（输入新称呼）"
4. 用户选择"就用这个" → 执行 `python scripts/profile_manager.py set <昵称> --json-file _tmp_profile.json` 保存
5. 用户选择修改 → 获取新昵称后执行 `python scripts/profile_manager.py set <新昵称> --json-file _tmp_profile.json` 保存
6. 写入成功后**自动清理临时文件**
7. 用 `python scripts/profile_manager.py list` 验证画像已创建

**自动昵称示例**：热恋小仙女、稳定摄影控、新婚美食家

#### 情况 2：有画像

> 🔴 **每次开启新会话，必须先展示所有已有画像名称，让用户选择操作方式。**

1. 运行 `python scripts/profile_manager.py list` 获取所有已有画像
2. **只展示画像名称列表**（不展示详情），格式：
```
📋 已找到以下画像：
1. [昵称1]
2. [昵称2]
3. [昵称3]
```
3. 用 AskUserQuestion 问："要怎么操作？"
   选项：
   - 使用已有画像 → 让用户选择具体哪个画像（动态生成选项："使用 [昵称1]"、"使用 [昵称2]"…）
   - 更新已有画像 → 让用户选择具体更新哪个画像 → 进入增量更新
   - 新建画像 → 进入情况 3

4. 用户选择"使用已有画像" → `get <昵称>` 读取并展示画像摘要 → 问"画像信息有变化吗？"（选项：没变化，继续 / 有变化，我来补充）

#### 情况 3：新建画像

> 🔴 **新建画像是完全独立的流程，不保留之前会话的任何记忆。必须从头走"情况 1：无画像"的完整流程。**

1. **清理旧会话数据**：删除当前 `memory.json`（如存在），确保新画像信息不掺杂之前会话的残留数据
2. **不读取旧画像字段**：新建画像时所有字段从零开始收集，不从已有画像继承任何信息
3. **跳转到"情况 1：无画像"的完整流程**：用 AskUserQuestion 问"你想怎么提供她的信息？"，给出四个选项（社媒ID/我来描述/发截图/逐题问答），严格按情况 1 的步骤执行
4. 保存为新画像（`profile_manager.py set <新昵称> --json-file _tmp_profile.json`），不影响已有画像
5. 后续 Step 2~7 均基于新画像执行

**画像完整度反馈**：画像保存后，告知用户当前画像完整度（如"画像完整度 70%，缺失：尺码、香调。补充这些信息可提升推荐精度"），但不强制补充。

**Step 1 完成标志**：画像已读取/创建并确认，有 `set` 保存或确认无变化。

---

### Step 2：关系阶段（Step 1 完成后立即执行）

> 📌 **存储约定**：`relationship_stage` 是画像的**持久字段**（见 `references/profile-schema.md`）。若画像已有该值，作为本次默认值带出确认；首条消息明示新阶段（如"热恋期""刚结婚"）则直接覆盖，不重复提问。

> 🔴 **此步必须在 Step 1 之后、Step 3 之前完成关系阶段写入。用户仅说“女朋友/老婆/心仪对象”不足以推断阶段，必须提问。**

> 🔴 **若首条描述中已明确给出或可无歧义映射到阶段（例如“热恋期”“交往半年”“刚结婚”），直接写入 `relationship_stage` 并跳过 AskUserQuestion，不再要求用户确认。只有缺失或歧义时，才使用 AskUserQuestion 单独提问；收到回答前不得进入 Step 3。**

即使用户在一开始说“给女朋友挑礼物”，也不能据此跳过此步；但首条消息明确标注“热恋期”等阶段时，应直接采用，无须二次确认。

> 🔴 **硬性规则：问题和选项必须与下方完全一致，禁止修改问题文本、禁止替换选项内容、禁止自行编造其他问法（如"女朋友/未婚妻/已婚"等）。只能使用以下 6 个选项 + Other。**

选项（必须原样使用，禁止更改）：
- 追求期（还在追求/暧昧，没正式在一起）
- 热恋期（刚确认关系 0-6 个月）
- 稳定期（交往半年以上）
- 新婚期（刚结婚 0-2 年）
- 周年纪念（每年固定纪念日）
- 长期陪伴（10 年以上）

**示例对话**：
用户："给女朋友挑七夕礼物"
你：Step 1 完成后，必须问 → "你们目前处于哪个阶段？"
→ 不能因为用户说了"女朋友"就推断是热恋期，必须提问确认

将解析结果或用户确认结果通过 `set` 写入 `relationship_stage` 字段。

**Step 2 完成标志**：`relationship_stage` 字段已通过 `set` 写入画像。

---

### Step 3：预算（Step 2 完成后立即执行）

> 🔴 **此步必须在 Step 2 之后、Step 4 之前完成预算写入。若首条描述中已有明确预算或预算区间，直接写入 `memory.json` 并跳过提问，不以预算建议覆盖用户给出的值。**

读取 `references/budget-advisor.md` 计算建议预算（阶段基准×关系深度×场景×历史修正），先展示：
```
💡 预算建议：建议区间 ¥[min]-[max]（[档位]），舒适上限 ¥[上限]
```

仅当用户未提供明确预算时，再用 AskUserQuestion 问："这次打算花多少？"
选项（动态生成）：建议区间 ¥[min]-[max] ← 推荐 / 精简些 ¥[low] / 隆重些 ¥[high] / 自定义（Other）

确定预算后写入 `memory.json`。

**Step 3 完成标志**：预算已写入 `memory.json`。

### Step 4：场景 + 自动推荐策略

> 🔴 **此步只允许问 1 个问题（送礼场合），禁止自行添加品类偏好、风格偏好、评分偏好等任何额外提问。**

#### 智能场合识别（先扫描用户输入，再决定是否提问）

**场合关键词映射**：

| 关键词 | 自动识别为 |
|--------|-----------|
| 七夕、情人节、圣诞、元旦、妇女节、母亲节、520、双十一 | **节日** |
| 生日、过生日、生日礼物 | **生日** |
| 纪念日、恋爱纪念、结婚纪念、周年 | **纪念日** |
| 道歉、哄、和好、求原谅 | **道歉** |
| 升职、乔迁、搬新家、毕业、考试结束 | **庆祝** |
| 随便送送、日常、没有理由、就想送 | **日常** |

**执行逻辑**：
1. **扫描用户输入**：查找上述关键词
2. **自动识别场合**：
   - 若找到明确关键词 → 直接设置场合，**跳过提问**
   - 若无法确定 → 用 AskUserQuestion 问："这次送礼是什么场合？"
3. **写入 memory.json**：记录识别到的场合

**示例**：
- 用户："帮我给女朋友挑**七夕**礼物" → 自动识别为 **节日**，跳过提问
- 用户："给女朋友挑礼物" → 无法确定，提问确认

**自动推荐策略**（无需用户选择，完全由系统自动决定）：

结合 `relationship_stage`（关系阶段）和场合，自动确定推荐品类组合（详见 `references/relationship-stages.md`）。

**画像驱动品类策略**（采集和排序时严格遵循，**绝对禁止询问用户品类偏好**）：
1. 当画像或小红书笔记中存在 `skincare_brands`、`makeup_brands`、`scent`、明确品牌或品类愿望等正向信号时，优先纳入对应品类候选。
2. 用户明确说"不需要香氛美妆"或其他排除项时，排除对应品类。
3. 若画像和小红书均未提供有效品类信号，从 `relationship-stages.md` 当前阶段的方向中选择至少 3 个不同品类；5-8 个候选中每个入选品类至少 1 件，单一品类最多 2 件。禁止把“首饰”为默认主类而生成一串项链、手链或耳环。
4. 仅当用户明确指定品类，或有效证据只支持一个品类时，才允许候选集中于同一品类；报告必须说明集中原因和证据来源。
5. 品类组合完全由画像字段、小红书口碑、关系阶段和场合自动推断，**全程不询问用户**；所有候选仍按综合分统一排序，不按品类分组。

采集时按上述策略自动多采集对应品类商品。报告排序时首饰排在最前。

**评分权重**（固定为均衡，**绝对禁止询问用户评分偏好**）：
- 价格合理性 20% / 质量 25% / 好评率 20% / 差评率反向 15% / 阶段适配度 20%
- 权重固定不变，**全程不询问用户选择评分偏好**
- 权重预设参考详见 `references/scoring-model.md`（仅供系统内部查阅，不对用户展示）

**Step 4 完成标志**：场合已确定，自动推荐策略已生效。

### Step 5：登录态自动检测与登录

> 💡 **运行环境说明**：若当前运行时未安装 Playwright（无浏览器自动化能力），直接走"跳过登录，用公开数据"路径（置信度低），不要阻塞流程——见下方"提供跳过出口"。

> 🔴 **登录策略**：两个渠道都登录可获取双渠道比价（置信度高）；只登录一个也能用（置信度中）；完全不登录可跳过用公开数据（置信度低）。**绝不强制要求两个渠道都登录。**

#### 状态标记说明：
- `CHECK_AND_LOGIN_STATUS:OK` → 登录态有效，跳过该渠道
- `CHECK_AND_LOGIN_STATUS:NEED_LOGIN` → 需要登录，浏览器将自动打开
- `LOGIN_STATUS:OK` → 登录成功
- `LOGIN_STATUS:TIMEOUT` → 登录超时
- `LOGIN_STATUS:ERROR` → 登录出错

#### 执行流程：

1. **逐个检测渠道登录态**（先淘宝，后京东，顺序不重要）：
   ```
   python scripts/login_manager.py check_and_login taobao
   python scripts/login_manager.py check_and_login jd
   ```

   读取每个渠道的状态标记，**已完成的渠道不重复检测**：
   - `CHECK_AND_LOGIN_STATUS:OK` → ✅ 该渠道有效，标记为"已就绪"
   - `CHECK_AND_LOGIN_STATUS:NEED_LOGIN` → 🌐 浏览器打开，提示用户登录
   - `LOGIN_STATUS:OK` → ✅ 登录成功，标记为"已就绪"
   - `LOGIN_STATUS:TIMEOUT` → ⚠️ 只记录该渠道失败，**不影响另一个已成功的渠道**
   - `LOGIN_STATUS:ERROR` → ❌ 只记录该渠道失败，继续检测其他渠道

2. **登录完成后反馈结果**，告知用户当前可用的渠道：
   ```
   登录检测完成：淘宝 ✅ 已就绪 / 京东 ⚠️ 超时（可跳过）
   ```

3. **提供跳过出口**：
   - 如果任一渠道失败，用 AskUserQuestion 问：
     "京东登录超时了，淘宝已就绪。要怎么处理？"
     选项：
     - 只用已就绪的渠道继续（置信度中）
     - 跳过所有登录，用公开数据（置信度低）
     - 我再试一次登录
   - 如果两个渠道都失败，问：
     "两个渠道都未就绪，是否跳过登录用公开数据？"
     选项：跳过，用公开数据 / 我再试一次

4. **已就绪的渠道保留**，只重试失败的渠道。超时后不要让用户重新登录所有渠道。

> 💡 **登录说明**：登录是为了获取实时价格和真实评价。只登录一个渠道也能用，只是比价维度减少（置信度降为中）。登录态存储在本机 `~/.workbuddy/gift-picker/sessions/` 目录，可随时用 `python scripts/login_manager.py clear <channel> --confirm` 清除，不会代你下单。

**Step 5 完成标志**：至少有 1 个渠道就绪，或用户选择跳过登录。已就绪渠道数决定置信度等级。

### Step 6：多渠道采集 + 评分 + 优质评论

> 🔴 **采集进度反馈（强制要求）**：每个阶段开始和完成时都必须向用户反馈进度。禁止长时间无反馈让用户以为卡死。反馈格式示例：
> ```
> 📊 阶段 1/4：小红书口碑挖掘中…（预计 2 分钟）
> ✅ 阶段 1 完成：找到 6 个候选商品，提炼好评点 8 条、差评点 3 条
> 📊 阶段 2/4：搜索引擎品类调研中…
> ✅ 阶段 2 完成：获取品类榜单 1 份、历史价格参考 3 条
> 📊 阶段 3/4：淘宝/京东比价中（3/8 商品进行中）…
> ⚠️ 京东价格抓取失败，该商品只展示淘宝价（置信度降为中）
> ✅ 阶段 3 完成：5 个商品获取双渠道报价，2 个单渠道报价
> 📊 阶段 4/4：补充源调研中…
> ✅ 阶段 4 完成
> 📝 评分中…
> ✅ 全部完成，正在生成报告…
> ```

> 🔴 **严格按照 `references/data-sources.md` 执行采集，不得跳过任何渠道或阶段。**

#### 阶段 1：小红书口碑挖掘（P0，无需登录）
- **必须实际打开小红书公开搜索页或笔记页**，按每个候选品类搜索：`<品类> 送女友`、`<品牌><品类> 真实测评`、`<品类> 踩雷`。不得只写出搜索关键词而不执行采集。
- 每个品类至少记录 2 篇可访问笔记的 URL、标题和可见互动数据；从评论区或正文提炼至少 2 条购买信号和 1 条避坑信号。将来源 URL、提炼内容和状态写入 `memory.json` 的 `xiaohongshu` 检查点。
- 正反两面都搜：种草词 + `踩雷/避雷/翻车`。只有带来源 URL 的笔记或评论才能作为小红书口碑写入推荐理由和评论区，禁止编造小红书评论。
- 若页面无法打开、要求登录、出现风控或没有可读内容，执行一次 `site:xiaohongshu.com` 公开检索；仍无结果则将状态写为 `unavailable`，不得静默跳过。报告的 `SOURCES_NOTE` 必须标注“小红书口碑未获取”，口碑维度降权。
- 产出：候选商品名单、口碑摘要（好评点/差评点各 5-10 条）、热度参考，以及 `opened`、`fallback` 或 `unavailable` 状态。

#### 阶段 2：搜索引擎品类调研（P0，无需登录）
- 品类榜单：`2026 <品类> 推荐`、`<预算>元 送女友 <场景> 礼物`
- 定向检索：`site:smzdm.com <商品名>`（历史价格）、`site:zhihu.com <商品名> 值得买吗`
- 产出：榜单商品列表、历史价格参考、测评文章摘要

#### 阶段 3：淘宝/京东价格与评价（P1，需登录）
- **淘宝**（需登录态）：搜索商品 → 按销量排序取头部 5-8 家 → 记录到手价、券后价、月销、店铺类型、评价区好评+差评各 5-10 条、**商品主图直接图片地址**、**商品详情页直接 URL**
- **京东**（比价层 + 优先图片源）：商品页免登可读价格与部分评价 → **优先采集京东商品主图**
- 每个候选商品必须获取 **淘宝 + 京东** 双渠道报价

> 🔴 **链接硬性规则**：
> - **图片地址**（`{{GIFT_IMG}}`）：必须采集 `<img>` 标签的 `src` 属性值，即直接图片 CDN 地址（如 `https://img10.360buyimg.com/n1/jfs/...`、`https://img.alicdn.com/imgextra/...`）。**禁止**使用商品页面 URL 或搜索页面 URL 作为图片地址。
> - **购买链接**（比价表"链接"列）：必须采集商品详情页的直接 URL（如 `https://item.jd.com/100028813901.html`、`https://detail.tmall.com/item.htm?id=677571166132`）。**禁止**使用搜索页 URL（如 `https://search.jd.com/Search?keyword=...`、`https://s.taobao.com/search?q=...`）。
> - 如果无法获取直接 URL（如页面结构变化），该渠道链接列标注"暂未获取，请自行搜索"并降低置信度，**不得**用搜索链接冒充商品链接。

#### 商品图片采集规则
- 每个候选商品必须采集至少 1 张主图的**直接图片地址**（`<img src="...">` 的值）
- **图片优先级**：京东主图 > 淘宝主图 > 搜索引擎图片
- 京东主图地址通常格式为 `https://img{N}.360buyimg.com/n1/jfs/...`，从商品页 `<img>` 标签的 `src` 或 `data-lazy-img` 属性中提取
- 淘宝主图地址通常格式为 `https://img.alicdn.com/imgextra/...`，从商品页主图 `<img>` 标签的 `src` 或 `data-src` 属性中提取
- 当两个渠道都有图片时，**默认采用京东商品图片**
- 若图片加载失败，报告模板自动显示 SVG 占位图降级

#### 阶段 4：可选补充源（P2，按需）
- `site:smzdm.com` 历史低价挖掘
- B站/微博视频测评口碑
- 降级路径：淘宝不可用 → 京东 + 搜索引擎聚合价（标注"非实时价"）

#### 评分与理由生成
按 `references/scoring-model.md` 五维评分（固定权重：价格20%/质量25%/好评20%/差评反向15%/阶段适配20%）。

每个候选生成 6 维推荐理由：
1. 阶段适配理由（引用 relationship-stages.md）
2. 画像匹配理由
3. 花语/寓意（限鲜花，引用 flower-guide.md）
4. 季节适配理由（引用 seasonal-guide.md）
5. 场景适配理由
6. 价格合理性（引用 budget-advisor.md）

**优质评论采集**（每个商品额外抓取）：
- 采集商品页面 2-3 条精选好评
- 优先选择带"精选"、"好评"、"正品"等标签的评论
- 提取评论者昵称（脱敏处理）、评分星级、评论摘要（50字以内）
- 评论作为报告的重要组成部分，增强推荐可信度

**Step 6 完成标志**：小红书检查点已记录为 `opened`、`fallback` 或 `unavailable`；5-8 个候选商品已完成多渠道采集和五维评分，并附带来源可追溯的优质评论。

### Step 7：报告生成

读取 `assets/report-template.html`，严格按 `references/html-spec.md` 中的「转义分层规则」填充占位符：
- **A 类（纯文本值）**：必须逐个 `html.escape()` 后写入
- **B 类（完整 HTML 片段）**：`{{RANKED_GIFTS}}`、`{{MATCH_TAGS}}`、`{{REASON_LIST}}`、`{{MATCH_LIST}}`、`{{PRICE_ROWS}}`、`{{FLOWER_BLOCK}}`、`{{REVIEWS_BLOCK}}`  **禁止整体转义**，必须在构造 HTML 字符串时对其内部每一段文本单独转义，然后把安全的 HTML 片段原样替换进占位符。若整段转义会出现截图中的 Bug——`<div>` 被当作纯文本显示而不是渲染成卡片。
- **C 类（系统枚举/数字）**：直接写入即可

**推荐排序**：
1. 按 `TOTAL_SCORE` 从高到低生成所有候选商品卡片，`RANK` 从 1 起连续编号；禁止按品类、价格区间或渠道重新分组。
2. 第 1 名使用 `top-pick` 样式和“最推荐”标记；第 2 名及之后使用“推荐第 N 名”标记。所有卡片默认展开并按排名连续展示，禁止折叠为“备选方案”。
3. 每张卡片保留商品图、价格、综合分、匹配点、推荐理由、风险提示和多渠道比价；每个商品展示 2-3 条精选评论，并将 1 条主要差评或风险提炼到风险提示。评论不足时明确标注数据不足，不得编造。
4. 不生成雷达图、品类横向对比或独立的“报告信息层次”区块。

**报告交付**：
1. 生成 HTML 文件 `gift-report-<YYYYMMDD-HHmm>.html`
   - `SOURCES_NOTE` 必须写明小红书口碑状态；当状态不是 `opened` 时，明确说明未获取原因和相应的置信度降级。
2. **同时输出一段纯文本摘要**（在对话中直接展示），格式：
   ```
   📋 报告摘要
   ─────────
   💝 最推荐：[商品名]
      价格：¥[min]-¥[max] | 综合分：[score]/100 | 画像匹配：[match]%
      亮点：[最关键的 1-2 个卖点]
      购买链接：[渠道名] [链接]
   
   后续推荐：第 2 名 [商品2] ¥[price]、第 3 名 [商品3] ¥[price]…
   
   ⚠️ 注意：[风险提示，如"鲜花需当日送达"、"某渠道只有一个报价"]
   ```
3. 告知用户："完整报告已保存为 HTML 文件，双击浏览器打开即可，手机上也能看（已适配移动端）。"
4. 交付成功后删除 `memory.json`

交付后提供一次轻量反馈机会：用 AskUserQuestion 问"推荐满意吗？"
选项：满意，就这样 / Top1 不太行，换一批 / 某个太贵了，调整预算 / 她可能有类似的了
用户反馈写入 `gift_history` 用于下次优化。

## 硬性规则

1. **7 步工作流严格有序**：Step 1→Step 2→Step 3→Step 4→Step 5→Step 6→Step 7；每步的数据处理必做，但首条描述已明确提供阶段、预算或场合时，跳过该字段的重复提问。
2. **登录态前置**：采集前必须通过 `login_manager.py check` 校验，失效则 `login` 让用户手动登录
3. **登录态失效熔断**：采集中发现失效立刻停止该渠道，重新校验后从 `memory.json` 断点继续
4. **XSS 防护（分层转义）**：按 `html-spec.md` A/B/C 三类处理——纯文本逐个转义，HTML 片段整体不转义但内部文本必须逐段转义，系统生成的枚举/数字无需转义。**严禁不分类型对所有占位符统一执行 `html.escape()`，否则会把摘要区三块卡片渲染成纯文本源码。**
5. **全免费**：只用免费信息源+用户本人登录态，禁止编造价格/销量
6. **清理**：报告交付后删除 `memory.json`

## 资源索引

| 文件 | 何时读取 |
|---|---|
| `references/profile-schema.md` | 建立/更新画像时的字段定义 |
| `references/relationship-stages.md` | Step 2 确认阶段 / Step 4 过滤品类 / Step 6 评分 |
| `references/flower-guide.md` | Step 6 生成花语建议 |
| `references/social-profiling.md` | 社媒 ID 一键画像详细流程 |
| `references/data-sources.md` | Step 6 采集 |
| `references/scoring-model.md` | Step 6 评分 |
| `references/budget-advisor.md` | Step 3 预算建议 |
| `references/seasonal-guide.md` | Step 6 季节理由 |
| `references/html-spec.md` + `assets/report-template.html` | Step 7 报告生成 |
| `scripts/profile_manager.py` | 画像读写 |
| `scripts/login_manager.py` | 登录态管理 |
