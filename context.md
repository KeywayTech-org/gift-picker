# gift-picker 当前上下文说明

`SKILL.md` 是唯一权威流程。本文只记录运行时接入边界，避免保留已删除脚本或旧版画像流程。

## 运行时能力

运行时按需提供以下能力：

- 当前浏览器实例及用户已有登录态；
- 小红书或微博的公开内容搜索和读取；
- 商品搜索、商品详情、价格、规格、店铺和正品依据读取；
- 公开搜索作为非实时降级路径。

能力不可用时，必须遵循 `SKILL.md` 中的预检、STOP / CHECKPOINT 和降级规则，不得猜测或绕过访问控制。

## 来源记录

所有外部事实都必须在会话中保留：

```text
source_url
source_type
observed_at
```

缺少任一字段的事实只能标记为未验证，不得参与排序或最终购买链接输出。

## 安全边界

只读取公开内容和用户已有登录态，不读取密码、Cookie、私信、收藏或联系方式；不发布、评论、点赞、下单，不写入项目、临时文件或用户记忆。

## 验证

```text
python scripts/validate_assets.py
```

该命令只验证静态资产、文档引用、敏感信息扫描和最小 dry-run 契约；它不能证明实时价格、库存、店铺资质、登录态或外部服务当前可用。
